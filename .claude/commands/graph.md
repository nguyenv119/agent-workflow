# Concept Graph

Update this session's concept/architecture graph with **$ARGUMENTS**, then show it.

Invoke the `graph` skill and follow its instructions. The skill and its helper
`graph.sh` live in `.claude/skills/graph/`. If `$ARGUMENTS` is empty, just build
and open the current graph (no edit).
