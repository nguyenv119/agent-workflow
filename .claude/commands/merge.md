# Merge Queue

Process open PRs — merge when CI passes, handle rebases, file issues for failures.

Run this in a dedicated window while other windows do `/work`.

---

@.claude/skills/merge-queue/SKILL.md
