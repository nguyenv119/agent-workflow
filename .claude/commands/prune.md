# Prune dead code

Run the prune skill for **$ARGUMENTS** (a workspace name, "all", or a phase like "controls" / "scan").

Read-only until deletion beads exist; deletion only through `/work`.

---

@.claude/skills/prune/SKILL.md
